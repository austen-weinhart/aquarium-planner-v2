<h1 id="species"><%= @fish.name %></h1> 

<div id="wiki" class="col-sm-7 box">
  <!-- Fish Wikipedia Profile -->
  <%= @fish_page.html_safe %> 
</div>

<div id="compatability" class="col-sm-4 box">
  <table class="table">
    <tr>
      <th>Species</th>
      <th>Compatability</th>
    </tr>
      <% @fish.attributes.each do |species, compatability| %>
        <tr>
          <td><%= species %></td>
          <td><%= compatability %></td>
        </tr>
      <% end %>
  </table>
</div>
